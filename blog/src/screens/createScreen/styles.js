import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    padding: 15,
    marginBottom: 15,
    borderRadius: 4,
  },
  label: {
    fontSize: 20,
    fontWeight: "bold",
  },
  container: {
    padding: 10,
  },
});
